/** Fixed navbar offset (py-2 + logo h-12/h-14 + border-b + buffer). */
export const HEADER_SPACER_CLASS = "h-[72px] sm:h-[80px]";

/** Pull page heroes flush under the fixed header (pair with HEADER_SPACER_CLASS). */
export const HERO_FLUSH_CLASS = "-mt-[72px] sm:-mt-[80px]";

/** Top padding so hero text clears the fixed navbar (use on flushed heroes). */
export const HERO_CONTENT_PT = "pt-[88px] sm:pt-[96px]";
