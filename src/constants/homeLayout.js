/** Shared layout for full-viewport home page sections (scroll-snap). */
export const HOME_SECTION_CLASS =
  "min-h-screen snap-start snap-always flex flex-col justify-center";

/** Dark navy → subtle purple gradient (replaces solid primary section backgrounds). */
export const DARK_SECTION_GRADIENT =
  "bg-gradient-to-br from-slate-950 via-slate-900 to-primary-dark/40";
