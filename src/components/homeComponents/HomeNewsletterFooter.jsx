import Footer from "../Footer";

/** Final home snap section: footer only (subscribe lives in footer column). */
export default function HomeNewsletterFooter() {
  return (
    <section className="snap-start snap-always shrink-0 font-inter">
      <Footer embedded />
    </section>
  );
}
